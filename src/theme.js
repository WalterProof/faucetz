import theme from "@theme-ui/preset-base";

export default {
    ...theme,
    container: {
        maxWidth: 768
    },
    buttons: {
        primary: {
            cursor: "pointer"
        }
    }
};
