import theme from "@theme-ui/preset-base";

export default {
    ...theme,
    container: {
        maxWidth: 768
    },
    breakpoints: ["40em"],
    buttons: {
        primary: {
            cursor: "pointer"
        }
    }
};
